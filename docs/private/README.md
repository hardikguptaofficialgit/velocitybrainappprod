# Private Repo

Private repository name: `velocitybrain-core`

This repo should contain:

- hosted FastAPI API
- reuse engine
- validation and adoption services
- backend auth and usage services
- dashboard
- storage and persistence logic

It should not publish a client package to PyPI.

